export interface InitialData {
    x: number;
    y: number;
    color: [number, number, number, number];
  }
  
  export interface Particle {
    x: number;
    y: number;
    r: number;
    color: string;
  }
  